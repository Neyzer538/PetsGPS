# Map Images Directory

Place map-related images here:

- `map-background.jpg` - Background image for the map view (recommended size: 800x600px or larger)

The application will fallback to `/src/assets/map.jpeg` if this image is not found.

